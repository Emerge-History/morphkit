**** Plugins ****

Plugin = middleware

-> [preprocessor -> ? x ] DATA STREAM [processor] ->



preprocessor(req, header, cb) {

...
    return cb(true / false);
}

processor(req, header, data_buf, res, cb) {
...

}

